<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_today_serie"
            android:title="@string/series" />
    </group>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_retentions"
            android:title="@string/leftMenuRetentions" />
    </group>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_tournament"
            android:title="@string/leftMenuTournament" />
    </group>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_playoff"
            android:title="@string/playoff" />
    </group>

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_bow"
            android:title="@string/bow" />
    </group>

    <!--
    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>
    -->

</menu>
